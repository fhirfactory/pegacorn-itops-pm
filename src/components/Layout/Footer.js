import React from 'react';
import { Nav, Navbar, NavItem } from 'reactstrap';

const Footer = () => {
  return (
    <Navbar>
      <Nav navbar>
        <NavItem>
          {/* placeholder */}
        </NavItem>
      </Nav>
    </Navbar>
  );
};

export default Footer;
